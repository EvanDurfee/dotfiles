# Load the common profile
if [[ -e ~/.config/profile/common ]]; then
  . ~/.config/profile/common
fi

# If the shell is interactive and bashrc exists, load it
if [[ $- == *i* && -f ~/.bashrc ]]; then
    . ~/.bashrc
fi
